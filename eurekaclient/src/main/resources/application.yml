server:
  port: 8010
spring:
  application:
    name: provider
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  #注册中心访问的地址
  instance:
    prefer-ip-address: true   # 是否注册ip